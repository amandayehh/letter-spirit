import Wall from "@/components/Wall";

export const metadata = {
  title: "Gridfont — the wall of a",
  description: "Every shared letter a, together.",
};

export default function Page() {
  return <Wall />;
}
